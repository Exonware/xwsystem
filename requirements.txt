# xSystem Requirements - 17 Core Lightweight Formats
# Company: eXonware.com
# Author: eXonware Backend Team
# Email: connect@exonware.com
# Version: 0.1.0.6
# Generation Date: 02-Nov-2025
#
# This file contains dependencies for xSystem's 17 core lightweight serialization formats (~10 MB)
# Enterprise formats (~87 MB) moved to exonware-xwformats for modular installation
# Requires Python 3.12+
#
# Usage:
#   pip install -r requirements.txt
#
# For enterprise formats:
#   pip install exonware-xwformats  # Adds 16 formats: Schema, Scientific, Database (enterprise)

# =============================================================================
# CORE DEPENDENCIES
# =============================================================================
# Source size (src/*): ~2.64 MB
pip                     # Package installer (CI and lazy install)

# =============================================================================
# CORE TEXT FORMAT DEPENDENCIES (Full-Featured: JSON, YAML, TOML, XML)
# =============================================================================

# YAML Support - Full Features (~3 MB)
PyYAML>=5.4.0                    # Standard YAML parser and emitter
ruamel.yaml>=0.17.0              # Advanced YAML with round-trip preservation
pyyaml-include>=1.3.0            # YAML includes support

# TOML Support - Full Features (~500 KB)
tomli>=2.0.0                     # TOML reading (Python <3.11)
tomli-w>=1.0.0                   # TOML writing
rtoml>=0.9.0                     # Fast Rust-based TOML parser
toml>=0.10.2                     # Legacy TOML parser (lazy installer runtime)

# XML Support - Full Features + Security (~2 MB)
lxml>=4.9.0                      # Fast XML/HTML parser with XPath/XSLT
xmltodict>=0.13.0                # XML ↔ dict conversion (requires >=0.13.0 for security features)
dicttoxml>=1.7.0                 # Optional fallback for encoding (not recommended for round-trip)
defusedxml>=0.7.0                # Secure XML (prevents XXE attacks)
xmlschema>=2.0.0                 # XML Schema validation

# JSON Support - Full Features (~2 MB)
orjson>=3.8.0                    # Ultra-fast JSON (2-3x faster than built-in)
ijson>=3.2.0                     # Iterative JSON parsing for large files
jsonschema>=4.17.0               # JSON Schema validation
jsonpointer>=2.3                 # JSON Pointer (RFC 6901)
jsonpatch>=1.32                  # JSON Patch (RFC 6902)
json5>=0.9.0                     # JSON5 (comments + trailing commas)

# =============================================================================
# CORE BINARY FORMAT DEPENDENCIES
# =============================================================================

# BSON Support - Full Features (~2 MB)
pymongo>=3.12.0                  # MongoDB BSON implementation

# Fast Binary Formats (~500 KB)
msgpack>=1.0.0                   # MessagePack (fast, compact)
cbor2>=5.0.0                     # CBOR - RFC 8949 binary standard

# =============================================================================
# SECURITY & CRYPTOGRAPHY DEPENDENCIES
# =============================================================================

cryptography>=3.4.0              # Cryptographic primitives and TLS
bcrypt>=4.0.0                    # Secure password hashing

# =============================================================================
# HTTP & NETWORKING DEPENDENCIES
# =============================================================================

httpx>=0.23.0                    # Modern async HTTP client
requests>=2.28.0                 # Classic HTTP library

# =============================================================================
# SYSTEM & MONITORING DEPENDENCIES
# =============================================================================

psutil>=5.9.0                    # System and process monitoring
aiofiles>=23.0.0                 # Async file I/O
setuptools>=65.0.0               # pkg_resources for lazy installer

# OpenTelemetry - Observability Stack
opentelemetry-api>=1.15.0        # OpenTelemetry API
opentelemetry-sdk>=1.15.0        # OpenTelemetry SDK

# =============================================================================
# TESTING DEPENDENCIES (Development)
# =============================================================================

pytest>=7.0.0                    # Testing framework
pytest-cov>=4.0.0                # Coverage reporting
pytest-asyncio>=0.20.0           # Async testing support
pytest-benchmark>=4.0.0          # Benchmarking (GUIDE_54_BENCH.md)

# Code Quality
black>=22.0.0                    # Code formatting
isort>=5.10.0                    # Import sorting
flake8>=4.0.0                    # Linting

# Type Checking
mypy>=0.950                      # Static type checking

# =============================================================================
# DEPENDENCY SIZE SUMMARY
# =============================================================================
#
# Core Dependencies (direct deps only): ~10 MB (90% reduction!)
# ├─ JSON full features: ~2 MB (orjson, ijson, jsonschema, etc.)
# ├─ YAML full features: ~3 MB (PyYAML, ruamel.yaml, pyyaml-include)
# ├─ TOML full features: ~500 KB (tomli, tomli-w, rtoml)
# ├─ XML full features: ~2 MB (lxml, xmltodict, dicttoxml, defusedxml, xmlschema)
# ├─ BSON full features: ~2 MB (pymongo)
# ├─ Binary formats: ~500 KB (msgpack, cbor2)
# └─ Security & Infrastructure: ~1 MB (cryptography, bcrypt, httpx, etc.)
#
# Enterprise Formats (exonware-xwformats): ~87 MB
# ├─ Schema formats: ~45 MB (protobuf, avro, parquet, thrift, orc, capnproto, flatbuffers)
# ├─ Scientific formats: ~35 MB (numpy, pandas, pyarrow, h5py, scipy, netCDF4, zarr)
# ├─ Database formats: ~6 MB (lmdb, neo4j, pydgraph, plyvel)
# └─ Binary formats: ~100 KB (py-ubjson)
#
# Lazy vs Full Modes (approximate, direct deps only):
# ├─ [lazy]: ~10 MB base footprint (core JSON/YAML/TOML/XML/BSON + xwlazy engine; heavy formats installed on-demand)
# └─ [full]: ~97 MB total (core ~10 MB + enterprise formats ~87 MB)
#
# Total savings vs full monolith: 90% smaller, 20–30x faster startup!
#
# =============================================================================
# ENTERPRISE FORMATS (Install exonware-xwformats)
# =============================================================================
#
# For enterprise/specialized formats, install:
#   pip install exonware-xwformats
#
# This adds 16 formats:
# - Schema (7): Protobuf, Avro, Parquet, Thrift, ORC, Cap'n Proto, FlatBuffers
# - Scientific (5): HDF5, Feather, Zarr, NetCDF, MAT
# - Database (3): LMDB, GraphDB, LevelDB
# - Binary (1): UBJSON
#
# Or use lazy loading:
#   pip install exonware-xwsystem[lazy]
#   # Auto-installs missing formats on first use!
#
# =============================================================================
# SCHEMA REGISTRY (Install exonware-xwschema)
# =============================================================================
#
# For schema registry features (AWS Glue, Confluent):
#   pip install exonware-xwschema
#
# This adds:
# - AWS Glue Schema Registry (boto3)
# - Confluent Schema Registry (requests)
#
# =============================================================================
pyzpaq
cachebox
cachetools
pylru
msgspec
hyperlight_hyperjson
brotli
zstandard
rich
attrs
py7zr
rarfile
lz4
pytz
diskcache
django
cacheout
