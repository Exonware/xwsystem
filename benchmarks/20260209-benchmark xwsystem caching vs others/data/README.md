# Caching benchmark data

- **baseline/_legacy/baseline_v0.0.1.387.json** - Legacy baseline (v0.0.1.387). Scripts may reference it in reports; new results are written to `../benchmarks/`.
- Temp dirs for TwoTierCache and diskcache runs are created under this folder (or system temp) when running the benchmark script.
